hey
  copy: cp
    [glob] to [filepath]: (files matching glob) [filepath]
      - if [glob] is one file, and [filepath] is a dir, use original name, otherwise use `basename` of [filepath]
    ...from...: (files matching glob) [filepath] (temp change cwd)

  create/make: touch or mkdir -p
    [path]: [path]
      -
    [filepath] in [path]: [filepath] (temp change cwd)
    file...: specify to make file instead of dir (if no ext)
    directory...: specify to make directory instead of file (if has ext)

  delete: (move to trash)
    [glob]: (files matching glob)
    ...from [path]: (files matching glob) (temp change cwd)

  find: find
    [glob]: (files matching glob)
    ...in [path]: (files matching glob) (temp change cwd)

  go: cd
    back: -
    home: ~
    to root: /
    to [path]: [path]
    ---
    there: used after create/make

  list: ls
    all: -lh
    [params]: [params]
    ...recursively...: -R
      - hey recursively list
      - hey list recursively
    ...from [path]: (temp change cwd)

  move: mv
    [glob] to [filepath]: (files matching glob) [filepath]
      - if [glob] is one file, and [filepath] is a dir, use original name, otherwise use `basename` of [filepath]
    ...from...: (files matching glob) [filepath] (temp change cwd)

  open: open
    [glob]: (files matching glob)
    ...from [path]: (files matching glob) (temp change cwd)

  where: pwd
    am i
    are we
